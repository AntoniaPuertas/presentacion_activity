<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Bienvenido_cocinas_MC_activity">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--Theme.Dialog muestra la activity como una ventana emergente-->
        <activity
            android:name=".ZoomActivity"
            android:theme="@style/TransparentCompat"
            android:parentActivityName="com.example.Bienvenido_cocinas_MC_activity.Ideas_cocinasActivity"></activity>
        <activity android:name=".Ideas_cocinasActivity" />
        <activity android:name="com.example.presentacion_activity.Aviso_legalActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>